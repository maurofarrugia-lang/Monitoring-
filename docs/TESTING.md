# Testing

Run `node tests/patterns.test.mjs` and `node tests/geometry.test.mjs`.

For PDF, test a text PDF and a scanned PDF. Confirm every exported page is present, non-blank and has correctly aligned redactions. For DOCX, confirm preview, finding toggles and multi-page flattened PDF export. Always inspect the final output manually.
