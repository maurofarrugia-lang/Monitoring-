let promise;export async function pdfjs(){if(!promise)promise=import('https://cdnjs.cloudflare.com/ajax/libs/pdf.js/4.10.38/pdf.min.mjs').then(m=>{m.GlobalWorkerOptions.workerSrc='https://cdnjs.cloudflare.com/ajax/libs/pdf.js/4.10.38/pdf.worker.min.mjs';return m});return promise}export async function openPdf(file){const m=await pdfjs();return m.getDocument({data:new Uint8Array(await file.arrayBuffer())}).promise}export async function renderPage(pdf,n,scale=2){const m=await pdfjs(),page=await pdf.getPage(n),viewport=page.getViewport({scale}),canvas=document.createElement('canvas');canvas.width=Math.ceil(viewport.width);canvas.height=Math.ceil(viewport.height);await page.render({canvasContext:canvas.getContext('2d',{willReadFrequently:true}),viewport}).promise;const content=await page.getTextContent(),items=content.items.map(i=>{const tx=m.Util.transform(viewport.transform,i.transform),h=Math.max(8,Math.abs(tx[3]||i.height*scale));return{text:i.str||'',x:tx[4],y:tx[5]-h,w:Math.max(2,i.width*scale),h}});return{canvas,items,text:items.map(i=>i.text).join('\n'),width:canvas.width,height:canvas.height}}
