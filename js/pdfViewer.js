let pdfJsPromise;
export async function getPdfJs(){if(!pdfJsPromise)pdfJsPromise=import('https://cdnjs.cloudflare.com/ajax/libs/pdf.js/4.10.38/pdf.min.mjs').then(m=>{m.GlobalWorkerOptions.workerSrc='https://cdnjs.cloudflare.com/ajax/libs/pdf.js/4.10.38/pdf.worker.min.mjs';return m});return pdfJsPromise}
export async function openPdf(file){const lib=await getPdfJs();return lib.getDocument({data:new Uint8Array(await file.arrayBuffer())}).promise}
export async function renderPage(pdf,pageNumber,scale=1.7){const lib=await getPdfJs(),page=await pdf.getPage(pageNumber),viewport=page.getViewport({scale}),canvas=document.createElement('canvas');canvas.width=Math.ceil(viewport.width);canvas.height=Math.ceil(viewport.height);await page.render({canvasContext:canvas.getContext('2d',{willReadFrequently:true}),viewport}).promise;const content=await page.getTextContent();const items=content.items.map(i=>{const tx=lib.Util.transform(viewport.transform,i.transform),h=Math.max(8,Math.abs(tx[3]||i.height*scale));return{text:i.str||'',x:tx[4],y:tx[5]-h,w:Math.max(2,i.width*scale),h}});return{canvas,items,text:items.map(i=>i.text).join('\n'),width:canvas.width,height:canvas.height,scale}}
