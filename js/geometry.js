import{IPA_LABELS}from'../patterns/index.js';
const norm=s=>s.toUpperCase().replace(/[^A-Z0-9]/g,'');
const union=items=>{const x=Math.min(...items.map(i=>i.x)),y=Math.min(...items.map(i=>i.y)),r=Math.max(...items.map(i=>i.x+i.w)),b=Math.max(...items.map(i=>i.y+i.h));return{x,y,w:r-x,h:b-y}};
function rowsOf(items){const rows=[];for(const item of items.filter(i=>i.text?.trim())){let row=rows.find(r=>Math.abs(r.y-item.y)<Math.max(6,item.h*.7));if(!row){row={y:item.y,items:[]};rows.push(row)}row.items.push(item)}rows.sort((a,b)=>a.y-b.y);rows.forEach(r=>r.items.sort((a,b)=>a.x-b.x));return rows}
function labelEnd(items,label){const target=norm(label);for(let start=0;start<items.length;start++){let joined='';for(let end=start;end<items.length;end++){joined+=items[end].text;const n=norm(joined);if(n===target)return end;if(n.length>target.length)break}}return-1}
export function textMatchBox(items,value){const target=norm(value);if(!target)return null;for(let i=0;i<items.length;i++){let joined='',used=[];for(let j=i;j<Math.min(items.length,i+10);j++){joined+=items[j].text;used.push(items[j]);const n=norm(joined);if(n===target||n.includes(target))return union(used);if(n.length>target.length*2)break}}return null}
export function ipaLabelBoxes(items,pageW,pageH){const rows=rowsOf(items),found=[];for(let ri=0;ri<rows.length;ri++){const row=rows[ri];for(const rule of IPA_LABELS){for(const label of rule.labels){const end=labelEnd(row.items,label);if(end<0)continue;const labelBox=union(row.items.slice(0,end+1));const same=row.items.slice(end+1).filter(i=>i.x>=labelBox.x+labelBox.w-2);let target=same.length?union(same):null;if(!target){const next=[];for(let j=ri+1;j<rows.length&&j<=ri+rule.rows;j++){if(rows[j].y-row.y>190)break;next.push(...rows[j].items)}if(next.length)target=union(next)}if(!target)target={x:labelBox.x,y:labelBox.y+labelBox.h+3,w:Math.min(pageW-labelBox.x-8,Math.max(150,labelBox.w)),h:rule.type==='Signature'?70:32*rule.rows};target={x:Math.max(0,target.x-5),y:Math.max(0,target.y-4),w:Math.min(pageW-Math.max(0,target.x-5),target.w+10),h:Math.min(pageH-Math.max(0,target.y-4),target.h+8)};found.push({type:rule.type,value:`Value near ${label}`,context:row.items.map(i=>i.text).join(' '),source:'IPA label geometry',box:target});break}}}return found.filter((f,i,a)=>a.findIndex(x=>x.type===f.type&&Math.abs(x.box.x-f.box.x)<8&&Math.abs(x.box.y-f.box.y)<8)===i)}
