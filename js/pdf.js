let lib;export async function pdfjs(){if(!lib)lib=import('https://cdnjs.cloudflare.com/ajax/libs/pdf.js/4.10.38/pdf.min.mjs').then(m=>{m.GlobalWorkerOptions.workerSrc='https://cdnjs.cloudflare.com/ajax/libs/pdf.js/4.10.38/pdf.worker.min.mjs';return m});return lib}export async function openPdf(f){const m=await pdfjs();return m.getDocument({data:new Uint8Array(await f.arrayBuffer())}).promise}export async function render(pdf,n,scale=1.7){const m=await pdfjs(),p=await pdf.getPage(n),v=p.getViewport({scale}),c=document.createElement('canvas');c.width=Math.ceil(v.width);c.height=Math.ceil(v.height);await p.render({canvasContext:c.getContext('2d'),viewport:v}).promise;const t=await p.getTextContent(),items=t.items.map(i=>{const x=m.Util.transform(v.transform,i.transform),h=Math.max(8,Math.abs(x[3]||i.height*scale));return{text:i.str||'',x:x[4],y:x[5]-h,w:Math.max(2,i.width*scale),h}});return{canvas:c,items,text:items.map(i=>i.text).join('\n'),width:c.width,height:c.height}}
