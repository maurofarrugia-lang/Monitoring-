export async function getPdfJs(){const m=await import('https://cdnjs.cloudflare.com/ajax/libs/pdf.js/4.10.38/pdf.min.mjs');m.GlobalWorkerOptions.workerSrc='https://cdnjs.cloudflare.com/ajax/libs/pdf.js/4.10.38/pdf.worker.min.mjs';return m}
export async function openPdf(file){const lib=await getPdfJs();return lib.getDocument({data:new Uint8Array(await file.arrayBuffer())}).promise}
export async function pageData(pdf,n,scale=1.55){const page=await pdf.getPage(n);const viewport=page.getViewport({scale});const canvas=document.createElement('canvas');canvas.width=Math.ceil(viewport.width);canvas.height=Math.ceil(viewport.height);await page.render({canvasContext:canvas.getContext('2d',{willReadFrequently:true}),viewport}).promise;const content=await page.getTextContent();const items=content.items.map(i=>{const tx=(await getPdfJs()).Util.transform(viewport.transform,i.transform);return{text:i.str||'',x:tx[4],y:tx[5]-Math.abs(tx[3]||i.height*scale),w:Math.max(2,i.width*scale),h:Math.max(8,Math.abs(tx[3]||i.height*scale))}});return{canvas,items,text:items.map(i=>i.text).join(' ').replace(/\s+/g,' ').trim(),width:canvas.width,height:canvas.height}}
